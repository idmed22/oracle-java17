package org.example.optional;

import org.example.data.*;

import java.math.BigDecimal;

public class InstanceOfExample {
/*
    public void printProduct(Product prod) {
        if (prod instanceof Drink drink){
            System.out.println("Product with name: "+drink.getName()+
                    " is Drink type.");
        } else if (prod instanceof Food food) {
            System.out.println("Product with name: "+food.getName()+
                    " is Food type.");
        }
    }

    public static void main(String[] args) {
        InstanceOfExample example = new InstanceOfExample();
        ProductManager pm = new ProductManager();
        Product p1 = pm.createProduct(100, "kefir", BigDecimal.valueOf(4.99), Rating.THREE_STAR);
        Product p2 = pm.createProduct(101, "English nuffin", BigDecimal.valueOf(1.99), Rating.FOUR_STAR);

        example.printProduct(p1);
        example.printProduct(p2);
        System.out.println("Is product 1 equals to product 2? " + p1.equals(p2));

        //reassign product
        p2 = pm.createProduct(100, "kefir", BigDecimal.valueOf(4.99), Rating.THREE_STAR);
        System.out.println("Is product 1 equals to product 2 after reassignment? " + p1.equals(p2));

    }


 */
}
